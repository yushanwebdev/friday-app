import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  image: {
    marginBottom: 20,
    width: 40,
    height: 40,
    alignSelf: 'flex-end',
  },
});

export default styles;
